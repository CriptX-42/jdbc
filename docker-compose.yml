version: '2.4'
services:
  db:
    image: mysql
    container_name: mysql-studant-class
    environment:
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3307:3306"
    volumes:
      - studant:/var/lib/mysql

volumes:
  studant: